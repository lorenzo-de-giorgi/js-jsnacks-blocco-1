let fridge = ['banana', 'mela', 'pera', 'ciliegia', 'arancia', 'mandarino', 'cocomero', 'limone', 'fragola'];
let pesca = fridge.push('pesca');

if (fridge.indexOf('cocomero') !== -1) {
    console.log('Cocomero presente');
} else {
    console.log('Cocomero non presente');
}